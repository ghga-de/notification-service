{
  "title": "ModSettings",
  "description": "Modifies the orginal Settings class provided by the user",
  "type": "object",
  "properties": {
    "email_notification_event_topic": {
      "title": "Email Notification Event Topic",
      "description": "Name of the event topic used to track email notification events",
      "example": "notifications",
      "env_names": [
        "ns_email_notification_event_topic"
      ],
      "type": "string"
    },
    "email_notification_event_type": {
      "title": "Email Notification Event Type",
      "description": "The type to use for events containing content to be sent as anemail notification.",
      "example": "email_notification",
      "env_names": [
        "ns_email_notification_event_type"
      ],
      "type": "string"
    },
    "service_name": {
      "title": "Service Name",
      "default": "ns",
      "env_names": [
        "ns_service_name"
      ],
      "type": "string"
    },
    "service_instance_id": {
      "title": "Service Instance Id",
      "description": "A string that uniquely identifies this instance across all instances of this service. A globally unique Kafka client ID will be created by concatenating the service_name and the service_instance_id.",
      "example": "germany-bw-instance-001",
      "env_names": [
        "ns_service_instance_id"
      ],
      "type": "string"
    },
    "kafka_servers": {
      "title": "Kafka Servers",
      "description": "A list of connection strings to connect to Kafka bootstrap servers.",
      "example": [
        "localhost:9092"
      ],
      "env_names": [
        "ns_kafka_servers"
      ],
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "email_notification_event_topic",
    "email_notification_event_type",
    "service_instance_id",
    "kafka_servers"
  ],
  "additionalProperties": false
}